## 1. Test infrastructure (TDD baseline)

- [x] 1.1 Add `sqflite_common_ffi` to `dev_dependencies` in `pubspec.yaml`.
- [x] 1.2 Create `test/test_helper.dart` that initializes `sqfliteFfiInit()` and sets `databaseFactory = databaseFactoryFfi`.
- [x] 1.3 ~~Characterization tests against CURRENT code~~ — **DEVIATION (pragmatic)**: rather than write throwaway tests against the old `toMap`/`fromMap` boilerplate that would need rewriting after freezed migration, the test suite was written once against the NEW code and includes legacy fixtures (e.g. `bodyPart = "CHEST&BACK&"`) to verify backward compatibility. See `test/domain/exercise_test.dart` for the legacy-token tests.
- [x] 1.4 Hostile-fixture test for legacy `SCREAMING_CASE` body parts — covered in `test/domain/exercise_test.dart`.

## 2. DB correctness fixes

- [x] 2.1 `onCreate`: replaced `forEach` with awaited `for...await` loop.
- [x] 2.2 Replaced every `"worklog"` / `"exercise"` literal with `workLogTable` / `exerciseTable` constants.
- [x] 2.3 Added `onUpgrade` hook; bumped `version: 1` → `version: 2`.
- [x] 2.4 `newWorkLog` dedup now uses an indexed `SELECT id FROM exercise WHERE name = ? LIMIT 1` query (in `_findExerciseByName`).
- [x] 2.5 Deleted unused `Map<String, dynamic> jsons` block in `backup()`.
- [x] 2.6 `getWorkLogByID` renamed to `getWorkLogById` and signature changed to `String id` matching the schema.
- [x] 2.7 Consolidated `updateExercise` / `editExercise` into one private `_saveExercise(Exercise, {required bool replaceBodyParts})`; the two public methods are thin wrappers preserving the old semantics.
- [x] 2.8 Wrapped `getExternalStorageDirectory()` in try/catch; throws typed `ExternalStorageUnavailableException` on null/error.
- [x] 2.9 `backup` / `restore` / `deleteWorkLog` / `close` all return `Future<void>`. Awaited at the call site in `exerciseView.dart`. Note: `backupView.dart` and `workLogPageView.dart` still call without `await` — flagged for proposal #3 which converts those screens to Riverpod and adds proper async-action handling. The new return type is `Future<void>` so the awaits can be added there without further DAO churn.
- [x] 2.10 Tests green.

## 3. Entity migration to freezed

- [x] 3.1 Added `freezed` (dev), `freezed_annotation` (direct) deps.
- [x] 3.2 `Exercise` is now a freezed sealed class. `Exercise.create(...)` factory preserves the old auto-generated-UUID semantics.
- [x] 3.3 `WorkLog` is now a freezed sealed class. `series` / `load` typed `Map<String, String>`. `WorkLog.create(...)` factory generates a UUID and defaults `created` to `DateTime.now()`.
- [x] 3.4 `fromMap` / `toMap` implemented as factory + instance method on each freezed class. `fromJson` / `toJson` generated by `json_serializable`.
- [x] 3.5 `getRepsSum`, `getReps`, `getLoad` rewritten to work on the immutable model (typed sum, null-safe lookup).

## 4. BodyPart rename

- [x] 4.1 `lib/entity/bodyPart.dart` rewritten with lowerCamelCase members.
- [x] 4.2 `Exercise.recreateBodyPart` replaced with `_decodeBodyParts` private helper using `BodyPart.values.byName(s.toLowerCase())` + `decodeBodyPart` public helper for single-token decode.
- [x] 4.3 `bodyPartsToString` replaced with `_encodeBodyParts` using `parts.map((b) => b.name).join('&') + '&'`.
- [x] 4.4 `Util.getBpColor` and `Util.getBpName` switches updated.
- [x] 4.5 Every call site updated (replace_all across `util.dart`, `exerciseManipulationView.dart`, `dbProvider.dart` seed list).
- [x] 4.6 Legacy `"CHEST"` token deserialization verified in `exercise_test.dart`.

## 5. Folder move

- [x] 5.1 `lib/util/dbProvider.dart` removed; new file at `lib/data/db/db_provider.dart` written from scratch (history transfer via `git mv` was not preserved because the rewrite was substantial enough that `git log --follow` would lose meaningful blame either way; the prior file is removed in this proposal's commit).
- [x] 5.2 Imports updated in `exerciseManipulationView.dart`, `workLogPageView.dart`, `exerciseView.dart`, `exerciseListView.dart`, `backupView.dart`. `helloWorldView.dart` and `calendarView.dart` did not import dbProvider directly.
- [ ] 5.3 ~~Rename `DBProvider.db` → `DBProvider.instance`~~ — **DEFERRED to proposal #3** where the singleton dies entirely (replaced by `dbProvider` Riverpod provider).

## 6. Tests for new requirements

- [x] 6.1 `test/data/db/db_provider_test.dart`: seed completeness, `newWorkLog` indexed dedup, body-part merge, `editExercise` vs `updateExercise` semantics, date-filtered queries, `deleteWorkLog`, `onUpgrade` schema-version hook installed.
- [x] 6.2 `test/domain/work_log_test.dart`: freezed equality, `copyWith`, `getRepsSum`, hostile mixed int/string `series` fixture, null bodyWeight coercion, `toMap` JSON encoding.
- [x] 6.3 `test/domain/exercise_test.dart`: freezed equality, `copyWith`, v4 UUID, legacy `SCREAMING_CASE` deserialization, unknown-token fallback, `toMap` encoding.
- [x] 6.4 All 27 tests green. Also deleted the broken `test/widget_test.dart` Flutter starter stub that tested a counter app this codebase never had.

## 7. Manual smoke + commit

- [ ] 7.1 **(DEFERRED — user will smoke-test after all 4 proposals)** Pull a `worklog.db` from a real device running v1.2.3+8, drop into the emulator's app sandbox, launch — verify no crash, all old workouts visible, all body parts colored correctly.
- [ ] 7.2 **(DEFERRED)** Add a new workout, force-quit, relaunch — new workout persists.
- [x] 7.3 `flutter analyze` → 0 issues. ✓
- [ ] 7.4 **(DEFERRED — one batch of commits at the end of all 4 proposals)** Split into commits per Migration Plan.
